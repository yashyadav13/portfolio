# Initialize git (if not already initialized)
git init

# Add all files
git add .

# Commit changes
git commit -m "Initial commit"

# Add remote repo (replace with your repo URL)
git remote add origin https://github.com/yashyadav13/portfolio.git

# Push to GitHub
git branch -M main
git push -u origin main
